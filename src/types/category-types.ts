export type CategoryProps = {
    id: string,
    title: string,
    user_id: string,
    created_at: Date
}

export type CreateCategoryProps = {
    id: string,
    title: string,
    userId: string
}